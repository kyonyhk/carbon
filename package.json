{
  "name": "carbon",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "tsc --noEmit -p packages/core && tsc --noEmit -p packages/cli && tsc --noEmit -p packages/server",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.1",
    "typescript": "^5.9.0"
  }
}
